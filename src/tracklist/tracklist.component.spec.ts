import { TestBed, async } from '@angular/core/testing';

import { TrackListComponent } from './tracklist.component';

describe('Track List Component', () => {
  it('should be named `TrackListComponent`', () => {
    expect(TrackListComponent['name']).toBe('TrackListComponent');
  });
});