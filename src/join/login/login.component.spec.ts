import { TestBed, async } from '@angular/core/testing';

import { SigninComponent } from './login.component';

describe('Sign In Component', () => {
  it('should be named `SigninComponent`', () => {
    expect(SigninComponent['name']).toBe('SigninComponent');
  });
});