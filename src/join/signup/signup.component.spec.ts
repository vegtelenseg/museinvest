import { TestBed, async } from '@angular/core/testing';

import { SignupComponent } from './signup.component';

describe('Sign Up Component', () => {
  it('should be named `SignupComponent`', () => {
    expect(SignupComponent['name']).toBe('SignupComponent');
  });
});